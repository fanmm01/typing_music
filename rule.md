# 《百日 finale》曲谱书写规范(整理版 rule.md)

> **定位**:本文件是 `.tymp` 曲谱书写的**唯一权威依据**。它合并了 `rule_0_2.txt`(基础规范)、`rule_0_3.txt`(0.3 版追加改动)的全部条款,并纳入实践中已确认但原文未记载的规则。
>
> **溯源标注**:
> - `[0.2:L38]` = rule_0_2.txt 第 38 行;`[0.3:L193]` = rule_0_3.txt 第 193 行。
> - `[实:P1]` = 实践确认的条款(来源 part-IV.txt 实证 + 作者确认),原文无对应文字者标注 **【新增条款】**。
> - rule_0_3.txt 正文与 rule_0_2.txt 同源(0.3 是 0.2 的修订 + 末尾追加章节),故两条溯源并存处表示 0.3 修订后的现行文字。
>
> **阅读顺序建议**:先读「一、总则」建立整体图景,再按需查「二~九」。
>
> **与规格文档的关系**:`tymp2musicxml_实现方案.md` 是实现本规范的技术方案,不得与本文件冲突;冲突时以本文件为准。

---

## 一、总则与结构综述

### 1.1 文件骨架

```
      左大括号前的行可以是任何内容,如位置参考。          [0.2:L12]
第 1 行   「标题 by 作者」                               [实:P1]
第 2 行   「instruments:结构id0 结构id1 …」              [实:P2]
第 3 行起 若干「组」:
     #timestamp:|||   .   .   .   |   …                [实:P3]
     { |拍号 |=调号 |速度 [&gt: {吉他调弦}] …          || 【组头行】
     chords,…:                                          || 【和弦行,可选】
     …                                                  || 【机动行,可选】
     …                                                  || 【数字行】
     …                                                  || 【歌词行,可选】
     [ …                                                || 【和声行,可多个】
     ( …                                                || 【伴奏行,可多个】
     }                                                  || 【组末行】
```

- 组头元数据行**不可省略**;但若曲谱共有多列,则从第二列(第二组)起,可空置组首元数据及行首元数据,此时沿用上一组的拍号/调号/速度/乐器/音区/吉他调弦。`[0.2:L30]`
- 用一个以 `}` 开头的行结束本组;`}` **之后**的内容同样视为本组的元数据(组末元数据)。`[0.2:L24]` `[0.2:L68]`

### 1.2 元数据区与 `||`

- 每一行都分为**元数据区**与**乐曲区**,分界为「组头元数据区结束后的两个 `||` 的结束点」。推荐每一行都在元数据区结尾添加 `||`,并与组头对齐,以免误记。`[0.2:L75]`
- **`||` 之前**是行头元数据(乐器名、音区、初始力度、特殊表记定义)。
- **`||` 之后**是乐曲区;乐曲区各行的**列号严格对齐**(见 §1.3)。`[实:P4]`

### 1.3 列与时间(核心坐标系统)【新增条款】

- 乐曲区以**列**为书写坐标;**1 列 = 16 分音符 = 1/4 拍**(以四分音符为 1 拍)。
- **每小节列数 = 16 × 拍号分子 ÷ 拍号分母**;每拍列数 = 16 ÷ 分母。
  - 4/4 → 每拍 4 列,每小节 16 列;6/8 → 每拍 2 列,每小节 12 列。`[实:P5]`
- 规则原文所述「每一个拍子由四字节组成」以 x/4 拍号为准。`[0.2:L116]`
- 「**单位**」一词在规则原文中有两种用法,本整理版区分如下:
  - **`[ ]` 类占位**以「**拍**」计:1 单位 = 1 拍(`[123]` 占 1 拍;`[~123]` 每个 `~` 多占 1 拍);
  - **`%` 类与特殊表记声明**以「**列**」计:1 单位 = 1 列 = 16 分音符(`%234` 占 4 列,4/4 下恰为 1 拍;6/8 下跨 2 拍,见附录 C-9)。
- 若某拍内等分数大于 5,该拍的书写宽度可以大于其列数。`[0.2:L119]`

### 1.4 动态行分类总则

- 曲谱记载中存在「**数字行**」「**机动行**」「**歌词行**」三种角色。`[0.2:L150]`
- **行数不固定、按内容动态判定**:
  - 主旋律与和声行的**行数均不限**;歌词只要位于某数字行的**正下方**即可被识别。`[0.3:L201]`
  - 一个伴奏声部行**不一定要占满三个格子**;随时间判断一行的内容是否(暂时)属于一个独立的伴奏行。`[0.3:L195]`
  - 一个伴奏声部**可能只占一行**,并临时借用旁边的行作为自己的机动行。`[0.3:L196]`
  - 据此,**机动行最多可以同时存在两个**(某数字行的上方一个、下方一个)。`[0.3:L197]`
- 两个数字行**可以紧邻**:此时只有机动行中的「单独符号」以及 `|` 开头的段落才被视为机动行的部分;否则那些数字将属于其所在的声部行。`[0.3:L191]`

---

## 二、组头元数据

### 2.1 拍号

- 只允许出现在每一组的首行,且**曲首必须存在**(不可省略)。`[0.2:L39-41]`
- 写法 `|6/8`,意为「从 `|` 所指出的小节起,拍号为 6/8」;`|` 可省略。`[0.2:L40]`
- 多列曲谱中,后续组可空置,沿用上一组。

### 2.2 调号

| 表示方法 | 写法 | 含义 |
|---|---|---|
| 方法 1(视同简谱) | `\|=G`、`\|=#G`、`\|=Dm` | 完全按简谱方式转调。和弦一般跟随做完全相同的转调,**除非**加后缀 `~`(`\|=#G~` 表示和弦行不跟随转调)。`[0.2:L44-45]` |
| 方法 2(固定变音) | `\|@[7-3-6-]` | **仅**使这些变音被固定;C 调为 `\|@[0]`。`[0.3:L193]` `[0.2:L46]` |

- 曲首应当存在调号,**缺省值为 `|=C`**。`[0.2:L47]`
- 方法 2 中,`|` 依旧可以由 `\` 代替(表示其数字行在其上方,见 §6)。`[0.3:L193]`
- 方法 2 可以在**数字行内**进行固定变音之增减(即中途改变)。`[0.3:L193]`
- 旧写法 `|-7-3-6` 等价于 `|@[7-3-6-]`(兼容保留)。`[0.2:L46]`

### 2.3 速度记号

- **绝对速度**:`|vb180` —— 从竖线对应拍子起,单个拍子的速度。`[0.2:L49]`
  - 曲首应当存在绝对速度记号,**缺省 120**。`[0.2:L50]`
  - 实践中由生成器写作浮点形式 `|vb120.000000`,解析器须接受小数。`[实:P10]`
- **速度变化记号**(以下均以 `|` 位置为准):`[0.2:L51-58]`
  - 延音号:`|。` 或 `|vt`。
  - 渐慢/渐快:`|vi-` / `|vi+`,或 `|rit` / `|rall` / `|accel`。
  - 结束:以下三者之一结束变速:
    - `|ve` —— 渐慢或渐快结束,以当前速度继续;
    - `|vr` —— 速度变化结束,回到最初速度;
    - `|vb180` —— 速度变化结束,变为此速度。若趋势方向正确(如 120 渐快到 180),则为渐快(慢)的终点;否则终点不受影响,抵达变速终点后突变到这个速度。
    - `|vb`,同 `|vr`。
  - 实践中存在带句点的写法 `|rit.` / `|vb.`,与不带句点者同义。`[实:P10]`
- **速度变化记号仅仅可以出现在组首与组末**,但它们不一定出现在行首。`[0.2:L68]`

### 2.4 力度记号

- 可写作 `|ppp` 到 `|fff` 之间;也可使用 `|s1` ~ `|s8`,分别代表**八种力度**。`[0.2:L60]`
  - 八级规定(本整理版固化,可整体替换):`s1=pppp`、`s2=ppp`、`s3=pp`、`s4=p`、`s5=mp`、`s6=mf`、`s7=f`、`s8=ff`。
- 渐强/渐弱类比渐慢/渐快,将 `v` 替换成 `s`。`[0.2:L61]`
  - 支持 `|cresc` / `|dim`;或使用两个小于号、两个大于号:`<   <` / `>   >`。`[0.2:L62-63]`
  - 第二个小于号/大于号可替换成或后紧跟力度编号;**第二个小于号/大于号后如果没有后文标注,回原处**;但如果是 `|se`,不做如此操作。`[0.2:L64]`
  - `|sr` / `|pr` / `|sb` / `|pb` 都被识别为相同的内容。`[0.2:L65]`
- 曲首力度记号可以不存在,此时**按力度为 4**。`[0.2:L66]`
- **作用域**:力度记号若对全局生效,需要放置在《组尾》;若放置在组头,仅仅对旋律们有效用。`[0.2:L67]`

### 2.5 和弦把位定义

- 格式:`{D/200232}` 类推。**先设定的会被后设定的在设定的那一拍及以后推翻**。默认为常用把位表。`[0.2:L70]`

### 2.6 特殊表记定义的位置【重要】

- 特殊表记的定义可以发生在**元数据行的任何位置**上,且其**定义域均为整个行**。`[0.3:L218]`
- 实践中(part-IV)22 个定义散布在 `{` 行 `||` 之后,与 `|rit.` / `|vb.` 等混排。解析器须**整行扫描**。`[实:P13]`
- 作用域:定义在乐器行头 → 本乐器;定义在组头/组尾 → 全局。查找顺序:本乐器 → 全局。

---

## 三、行头元数据

### 3.1 乐器名

- 除了**主旋律线路**与 `[` 开头的线路外,都需要存在乐器名。`[0.2:L77]`
- 中文与英文都是可以接受的,用 `<>` 分割(如 `Nylon Guitar<古典尼龙吉他`)。`[0.2:L77]`

### 3.2 初始音区

- **现行写法** `>4`(`[0.3:L223-224]`);**旧写法** `*4` / `*5` 兼容保留(`[0.2:L78]` `[实:P2]`)。
- 含义:该行**调根音 1 所在的八度**(八度编号以中央 C = C4 计)。
- 音区标注允许出现在**正文内部的数字行或机动行**:`>4|`(数字行)、`|>4|` 或 `\>4|`(机动行),代表从此处起为第四音区。`[0.3:L223-224]`
- `[` 和声行若无音区标注,继承主旋律。

### 3.3 初始力度

- 行头可含初始力度,写法同 §2.4。`[0.2:L79]`

### 3.4 特殊表记定义

特殊表记分五类。定义形态:

| 类 | 形态 | 说明 |
|---|---|---|
| ① 瞬时 | `x~{135}`、`x～{一三五}`、`x～{CEG}` | 被视为**一个音符**;括号内为同刻和弦(简谱数字 / 中文数字 / 绝对音名)。`[0.2:L83]` |
| ② 多瞬时 | `x&"_{358}/7/3-6-"` | `_` 为空格替代,`/` 划分拍。`[0.2:L84]` |
| ③ 吉他节奏型 | 同 ②,但内容使用小写 `a`~`f`;二者可以混合。`[0.2:L85]` |
| ④ 特殊和弦定义 | `{Cmy/300353}` | 不产生事件,登记"和弦名→指板把位"供 ③ 查询。`[0.2:L86]` |
| ⑤ 任何和弦节奏型 | `x&r"{358}7654345"` | `r` 表示内容为**和弦相对音**。`[0.2:L87]` |

**⑤ 类定义全内容**:`[0.2:L88-100]`

```
                 |标记名。可以是 1-4 个大/小写字母,字母数需 ≤ 被定义内容的总字符数。
                 |标记名不可以重复!不可以包含 ABCDEFG。开头与结尾不能是数字。
                 |柱式和弦。
                      |由于出现在括号内,所以代表一个音:如果 7 是和弦内音,演奏七音;
                      |如果不是,演奏 8 音,即高一个八度的和弦根音。
x&r"{358}(7/8)(A-)[56]4345"@[6-7-3-]
                    |该拍子时值被平分。
               |绝对音"A-"。  |标记变音:降 MI、LA、TI.
  |区分引号中采用和弦相对音 / 绝对音。
```

注意:
1. 和弦内音(1,3,5,7,8,9,…)总是和弦中的音;和弦外音总采用调内音。若需其他调外音插入,采用以下手段:`[0.2:L103-106]`
   - a. 在使用处写作 `x@|3-6-7-4+` / `x@|E-A-B-F+`(代表**绝对音的变音**);
   - b. 在机动行对此声部先进行变调;
   - c. 手动写一种(如分开记录大调与同主音小调)。
2. 如果在同一组和弦音之内和弦切换了,将会**重新开始节奏组**。**这条规则只适用于该组是一个"和弦",即定义处有 `r`**。`[0.2:L107-108]`
3. 数字可以替换成 A~G 来实现穿插绝对值;数字中可以存在 `+`/`-` 代表在调基础上使得该音变音,字母也可以。`[0.2:L88-89]`

**【新增条款·实践确认】**

- **无引号形态**:定义可写作 `Q&{13}_{13}_{13}_`(无引号),与 `&"…"` 同义。`[实:P7]`
- **参数化形态**:`X(V)&r"V_{35}_"` —— 解析时,将输入的 `X(V)` 中 `V` 的**具体内容**走入内部(文本代入)。注意:`V` 指的是**绝对的唱名**,而不是和弦根音/三音。`[0.3:L211-213]`
- **未闭合引号容错**:定义引号未闭合时,取至行尾并告警(实践中 `J&r"1353531'3.3'5'5.` 即此情形)。`[实:P21]`

### 3.5 伴奏声部行头(多种形态,解析器须全部接受)

| # | 形态 | 说明 |
|---|---|---|
| 1 | `(Accomponiment#0:Inst.#0<Instrument#0; Initial pitch range: >4` | 生成器标准形态 |
| 2 | `(Accomponiment#1:Inst.#1<Instrument#1;                      >4` | 省略 `Initial pitch range:` 关键词的对齐式 |
| 3 | `(-Inst.#0-` | 续组的**短行头**,沿用上一组同 id 的完整信息 |
| 4 | `(-Inst.#0-Vln1` + 下一行仅含乐器全名(如 `Violin`) | 双行行头,下一行的名称并入本行乐器名 |
| 5 | `*#1:Inst#1<Instrument1$1` | **简洁定义**(见下) |
| 6 | `*Vln\|` | **谱内就地定义**(见下) |
| 7 | (无任何行头) | **视为钢琴** `[0.3:L198]` |

**简洁定义** `*#1:Inst#1<Instrument1$1`:`[0.3:L202-206]`
- 最前面的 `#1` 可以省略;
- `<Instrument1` 可以省略;
- `$1` 代表该行的**音色号**,同样可以省略;**未指定时率先搜索 `Inst#1` 此处代指的内容,再不出现则填充钢琴**。
- 该定义可以完成在乐谱内部。最简便的方式是:在内部出现 `*Vln|` 并直接接续正文;**系统需要自动完成编号、指定 id 等等的操作**。`[0.3:L207-208]`

**ID 独一化**:更改 ID 标识使之独一化的责任,已**从新建文件工具改动到解析其并将其转换为 musicXML 的过程中**。`[0.3:L209]`

### 3.6 无音高打击乐器

- 无音高打击乐器行如果是**鼓组**,则映射到**鼓谱**(常用);否则任何数字**同等解读为一次发音**。`[0.3:L217]`

---

## 四、曲谱记载

### 4.1 事件记号

| 记号 | 含义 | 溯源 |
|---|---|---|
| `1` ~ `8` | 简谱数字(音级) | `[0.2:L121]` |
| `0` | 从其位置开始,**停止该音符**(休止至下一事件) | `[0.2:L122]` |
| `(1+)` | 标注括号里的内容是**同一个音符**,且**仅有这个音符**(不补空格)。**非常不建议用于数字行**;若要用,请在其他声部追加 `(/)` 占位 | `[0.2:L123-125]` |
| `[123]` | 括号内内容占**同一个单位**(1 拍),内部 n 等分 | `[0.2:L126]` |
| `[~123]` | 每个 `~` 多占一个单位(即以 2 拍做 3 等分) | `[0.2:L126]` |
| `{12}` | **同时演奏双音**(柱式和弦) | `[0.2:L127]` |
| `%234` | 把**四个单位(4 列 = 4 个十六分)**三连音化;4/4 下即"一拍三连音",6/8 下跨 2 拍(见附录 C-11) | `[0.2:L130]` |
| `%%234` | 把**八个单位(8 列)**三连音化 | `[0.2:L130]` |
| `%12345`、`%1234543` | 把**八个单位(8 列)**5 / 7 连音化 | `[0.2:L130]` |
| 特殊表记标记名 | 本身是一个事件 | `[0.2:L120]` |
| 两个单位三连音化 | 需要参照「机动行」一节 | `[0.2:L132]` |

**约束**:以上记号的**总占用位置数必须小于等于拍子数**,否则无法表示,需要借用机动行。`[0.2:L128]`

### 4.2 后缀标记

以下标识**均为后缀**,需要**占用下一个四分之一拍的谱位**;防止这么做的方式是「音符与标识前后各加一个 `_`」。`[0.2:L133]`

| 标记 | 含义 | 溯源 |
|---|---|---|
| `*` | 从此标识**前面的那个音**开始,上升一个八度组 | `[0.2:L134]` |
| `.` | 从此标识**后面的那个音**开始,下降一个八度组 | `[0.2:L135]` |
| `+` | 升号 | `[0.2:L136]` |
| `-` | 降号 | `[0.2:L137]` |
| `～` | 还原号 | `[0.2:L138]` |

**【新增条款·实践确认】**`'`(撇号)= **上升一个八度组**,作用同 `*`(作用于其前面的音)。实践中大量使用(旋律行、表记定义内、管道实例内、机动行内),而原文未记载。`[实:P1]`

### 4.3 八度判断规则

- **纯四度(含)以内,默认连续,无需升降八度。**`[0.2:L140]`
- 超出纯四度时,取最近的八度;两侧距离相同时取下行的连续进行(详见实现方案 §7.5)。

### 4.4 固定变音增减【0.3 新增】

- 可以在**数字行**进行固定变音之增减。`[0.3:L193]`
- 标记符号为 `|@[3-6-7-]`(或 C 调:`|@[0]`)。`[0.3:L193]`
- 竖线依旧可以由**反斜杠**代替,以表示其数字行在其上方(见 §6.2)。`[0.3:L193]`
- `|@[0]` 表示清空全部固定变音(回到无固定变音)。

### 4.5 `=` 替换格式

（仍以规则原文例子说明）`[0.2:L170-175]`

```
（机动行）   |32 |%654    |__1234535676782171  |3/2176714/321712
（数字行）124=317=5      =  =               345====
```

- 第一个 `=` 被取代为两个音符 `32`;
- 第二个 `=` 被三连音 `%654` 取代;
- 多个 `=` 连用,只需标注起点和终点即可,但也可以连续标注,**都可读**。
- 对于第三个,机动行中的音符标记可以通过在 `|` 后紧随一个或数个下划线 `_` 来表示:**其内部的时值划分与其外部不同**。如示例 3,便将该四个空格代表的时值拆成了 16 个格子。`[0.2:L177]`
- 也可以省略下划线,此时将**自动平分**时值。`[0.2:L178]`
- 机动行内的空格**被斜杠 `/` 代替**,原因是空格会被用于区分两处内容。`[0.2:L179]`

### 4.6 行复制记号 `@lN`【0.3 新增】

- `@l9` 发生在**任意数字行**中,表示「**从此以后,直到下一次某事件覆盖了这种状态**,该声部均存在,且复制文档总第 9 行的内容」。`[0.3:L222]`
- `N` = **文档总行号**(1 基,含标题行与所有行)。
- 实现口径(方案拟定,见附录 C):从记号所在列起按列对齐复制源行内容;本声部自身事件在列上覆盖被复制内容;复制内容按**本声部**的音区锚点重新消解;越界告警忽略;循环引用检出后截断并告警。

### 4.7 `-` 显式结束记号【新增条款】

- 数字行中的 `-` 单独出现时,表示**显式结束正在进行的节奏型**(停止当前由特殊表记或 `@lN` 建立的持续状态)。`[实:P16]`
- 无进行中的状态时忽略并告警。
- 实践中该记号可能与其后的标记名连写(如 `-H`),应拆为 `-`(结束)+ `H`(标记调用)两项。

---

## 五、歌词

- 歌词行一般与曲行呈现**一对一**的样态;但歌词行中的 `-` 将成为**延音线**。`[0.2:L142]`
- 歌词行也可被用于调节力度符号。`[0.2:L143]`
- 和声行的旋律行**下方**被视为该行的歌词:分拍子地填充,**且若该拍子内并无歌词视同上文**。`[0.2:L18]`
- 第一个歌词行**不能以数字开启**。`[0.2:L16]`

**【新增条款 · 0.3 与实 practice 确认】**

- **行数不限、动态识别**:歌词**在数字行正下方**即可被识别,与机动行一致是动态的。`[0.3:L201]`
- **歌词分隔符**:
  - `*l""` 或 `*l|""` —— 引号内部的文字,即被识别为(**上方一行**)的歌词;`[0.3:L202]`
  - `*l\""` —— 如果是**下方**那行数字行的歌词,采用 `\` 代替 `|`。`[0.3:L202]`
- **歌词内容原样输出**:引号内的任何字符(含标点、空格、符号)原封不动输出。`[实:P20]`
- 引号未闭合时取至行尾并告警。`[实:P21]`
- 实践中歌词行可能没有 `||`(如 `/*lirics*/ *l|" "`),此时以行头 token 推定元数据区终点。`[实:P14]`
- 旧式「裸按列歌词」写法(无 `*l` 前缀,歌词文字直接按列书写)继续兼容。`[0.2]`
- 注意:实践中该行头的注释拼写为 `/*lirics*/`(非 `lyrics`),解析器需接受两种拼写。`[实:P11]`

---

## 六、机动行

### 6.1 基本性质

- 机动行内的内容**被视作从属于数字行**。`[0.2:L159]`
- 例:`.` 在上、`6` 在下,该标记被视为「6 音,该音附带降一个八度组」;`+` 在上、`5` 在下,即「升 5 音」。`[0.2:L161-164]`
- 机动行内可以存在任意力度符号等等的标记,也可以出现前面提到的后缀、调号等。`[0.2:L166]`
- 机动行内的**空格被斜杠 `/` 代替**。`[0.2:L179]`
- 机动行中还可以看到:吉他节奏型定义、特殊和弦把位定义、一切大于等于一个瞬时的特殊表记。`[0.2:L183-186]`

### 6.2 方向语义(上指 / 下指)【本整理版规范表述】

- **凡涉及多个符号的标记,都必须以 `|` 开头(上指)或以 `\` 开头(下指)**,且将被视为在该对应符号对应之位置。`[0.2:L167-168]` `[0.3:L192]`
- **规范含义**(作者确认):
  - **`|`(上指)= 该机动内容位于数字行之上,服务于其下面一行。**
  - **`\`(下指)= 该机动内容位于数字行之下,服务于其上面一行。**
  - 二者互为相反方向;机动内容可写在数字行的上方(用 `|`)或下方(用 `\`)。
- **单独符号**(仅一个符号、无数字串)的归属:
  - 属「在下的机动行」(位于数字行下方)时,**必须位于 `\` 与 `\` 之间**;
  - **反斜杠从来不可以省略**。`[0.3:L192]`
  - 属「在上方的机动行」时直接书写(与数字行同列对应)。
- **两个数字行紧邻**时:只有机动行中的「单独符号」以及 `|` 开头的段落才被视作机动行的部分。`[0.3:L191]`
- **例(part-IV 实证)**:
  - 行 16 的 `|{42}_{31}_` 位于行 17 之上,与行 17 的 `====X` 同列配对 —— `|` 在上、服务下方 ✔ `[实:P4]`
  - 行 19 的单独符号 `'`、`.'`、`|@[0]` 位于行 20(密集数字行)之上,服务行 20 ✔
  - 行 21 的 `\@[3-6-7-]`、`\ ' .' . \` 位于行 21、服务其**上方**的数字行 ✔(下指)

### 6.3 组合与限制

- 一个机动行最多同时存在两个(某数字行的上、下各一)。`[0.3:L197]`
- 一个伴奏声部**可能只占一行**,并**临时借用旁边的行**作为自己的机动行。`[0.3:L196]`
- 数字行**自身也可以携带机动段**(如 part-IV 行 17 的 `|{72}_{61}_{5+7}`),此时该段服务其下方数字行的同列内容。`[实:P4]`

---

## 七、和弦行

- 可选行。用空行代替、或使之不存在直接接,都是可以被接受的。`[0.2:L14]`
- 实践中行头形如 `chords,eg.C,Am,Bmin7:`,`||` 之后为和弦序列。`[实:P12]`
- **`-` 表示该位置无和弦。**`[实:P12]`
- **和弦根音位置标注 `_`**:`G_6`、`F_46`、`Em7_34`、`Bmin7_56`、`bBmaj7_2`,其根音分别是 7、1、7、2、6。`[0.3:L226-228]`
- **和弦诸音的定义**:`[0.3:L229-233]`
  - 「**3 音、5 音、7 音**」(对于七和弦)指的是和弦从根音向上的第 **2、3、4** 个音;
  - 「**2 音、4 音、6 音、7 音**」(对于三和弦)则分别指**紧邻 1 音在 1 音上方、紧邻 3 音在 3 音上方、紧邻 5 音在 5 音上方、紧邻 8 音在 8 音下方**的调内音。
- 和弦符号由「根音字母 + 可选 `#`/`b` + 品质」构成;品质实践取值含 `m`、`min`、`maj7`、`7`、`dim`、`-64` 等。多个和弦可紧排书写(`Cm    Em  G Dm_6  F`),一个 token 即一个和弦。`[实:P12]`
- 和弦行的作用:为 ⑤ 类特殊表记提供和弦相对音的消解依据;也可输出为 MusicXML 的 `<harmony>`。

---

## 八、特殊表记展开细则

### 8.1 各类展开

| 类 | 展开 |
|---|---|
| ① | 展开为**一个**事件,内部 n 个同刻音(柱式和弦)。内容为简谱数字 → 按当前调消解;中文数字 → 同简谱;大写字母 → 绝对音。 |
| ② | 按 `/` 分拍;`_` 分隔单位;`{…}` = 该拍柱式和弦;单个数字 = 单音;`3-` 等 = 带变音音级。 |
| ③ | 同 ②,但 `a`~`f` = 琴弦名:该拍有活跃和弦 → 按把位表(④ / `{D/200232}` / 默认常用把位表)取弦上音;无和弦 → 按 `&gt:` 吉他调弦表取空弦音。 |
| ④ | **不产生事件**;登记「和弦名 → 把位」供 ③ 查询。 |
| ⑤ | 数字为**和弦相对音**(1,3,5,7,8,9… = 和弦内音;其他 = 调内音);`(7/8)` = 7 是和弦内音则奏七音、否则奏 8 音;`[56]` = 该拍时值被平分;`@[6-7-3-]` = 标记变音(降 MI/LA/TI);穿插 A~G = 绝对音;使用处 `x@|3-6-7-4+` = 临时绝对变音。 |

### 8.2 自动重复语义【0.3 新增】

- **大于一个单位时长的标记将自动被重复。**`[0.3:L214]`
- 如果引源是一个**和弦**(定义处有 `r`),那么**每当和弦被更换,其循环将重头开始**。`[0.3:L214]`
- **等于一个单位时长的标记将不会被重复,而是一次性。**`[0.3:L215]`

### 8.3 标记名规则

- 1~4 个大/小写字母;字母数需 ≤ 被定义内容的总字符数;`[0.2:L93]`
- **不可以重复**;不可以包含 ABCDEFG;开头与结尾不能是数字。`[0.2:L94]`
- 参数化定义(如 `X(V)`)与同名普通定义可共存;调用时以 `X(3)` 形式给出参数。

---

## 九、伴奏声部与乐器实装

### 9.1 声部排列

- 伴奏声部行数**灵活**:不一定要占满三个格子,按内容随时判断一行的内容是否(暂时)属于一个独立的伴奏行。`[0.3:L195]`
- 一个伴奏声部可能**只占一行**,并临时借用旁边的行作为自己的机动行。`[0.3:L196]`
- **一个伴奏声部如果没有在行首找到乐器定义,视为钢琴。**`[0.3:L198]`
- 最严谨的方式是在每一行行首都存在乐器定义行,代表此行的乐器。`[0.3:L199]`

### 9.2 数据结构实装清单【0.3 新增】

需在数据结构中实装:歌名作者、力度记号、速度记号、变速记号、和弦行、无音高打击乐器。`[0.3:L216]`
- 无音高打击乐器行:是**鼓组** → 映射到**鼓谱**(常用);否则任何数字**同等解读为一次发音**。`[0.3:L217]`

### 9.3 ID 独一化

- 独一化责任在**解析并转换为 musicXML 的过程中**。`[0.3:L209]`
- 建议口径:先做 XML NCName 清洗(`#`、`:`、`(` 等替换为 `-`),再对重名追加 `-2`、`-3`…;谱内就地定义(`*Vln|`)使用自动编号。

---

## 十、附录

### 附录 A —— rule_0_2 → rule_0_3 变更对照表

| # | 位置(rule_0_3.txt) | 改动 |
|---|---|---|
| 1 | L190-191 | 机动行归属:两数字行可紧邻;仅「单独符号」与 `\|` 开头段落属机动行 |
| 2 | L192 | 新增 `\` 下指定界符;单独符号须夹于 `\…\`;反斜杠不可省略 |
| 3 | L193(正文 L46) | 固定变音改版为 `\|@[3-6-7-]` / `\|@[0]`;数字行内可增减 |
| 4 | L194-209 | 伴奏声部排列灵活化;行首无乐器定义视为钢琴;新增简洁定义 `*#1:Inst#1<Instrument1$1`;ID 独一化责任移交解析器 |
| 5 | L201-202 | 和声/主旋律行数不限;歌词动态识别;新增歌词分隔符 `*l""` / `*l\|""` / `*l\""` |
| 6 | L211-213 | 参数化和弦定义 `X(V)&r"V_{35}_"`,V 为绝对唱名代入 |
| 7 | L214-215 | 特殊表记自动重复语义(>1 单位重复;r 类随和弦更换重头;=1 单位一次性) |
| 8 | L216-217 | 数据结构实装歌名作者/力度/速度/变速/和弦行/无音高打击 |
| 9 | L218-219 | 特殊表记定义可在元数据行任何位置,定义域整行 |
| 10 | L222 | 新增 `@lN` 行复制记号 |
| 11 | L223-224 | 音区前缀 `*` → `>`,允许出现在正文内部(`>4\|` / `\|>4\|` / `\>4\|`) |
| 12 | L226-233 | 和弦 `_` 根音位置标注 + 和弦诸音定义 |
| — | 正文 L167-168 | 机动行指位补 `\` 下指说明 |

### 附录 B —— part-IV 实践实例索引

| # | 实践写法 | 位置 | 对应本文件条款 |
|---|---|---|---|
| P1 | `'` 升八度组后缀 | 行 6/7/17/18/19/21/24 等 | §4.2【新增条款】 |
| P2 | `>4` 音区前缀(与 `*3`/`*4` 并存) | 行 7/17/20/23/26/29 | §3.2 |
| P3 | `@l7` / `@l9` | 行 18/24/29/30 | §4.6 |
| P4 | 管道柱式和弦 `\|{42}_{31}_` | 行 16/17 | §6.3 |
| P5 | 管道括号等分 `\|[{31'}_{27'}_{16-}_{75'}_]` | 行 17 | §4.1 + §6 |
| P6 | 参数化定义与调用 `X(V)&r"…"` / `X(3)` | 行 4 / 行 17-18 | §3.4 |
| P7 | 无引号定义 `Q&{13}_{13}_{13}_` | 行 4 | §3.4 |
| P8 | 定义内 `'`/`.`/`8`/`0`/`_`/`(7/8)`/首导根音 `1_` | 行 4 | §3.4 |
| P9 | 等号与标记名连用 `====X`、`=T(4)`、`=R` | 行 17-18 | §4.5 + §8.2 |
| P10 | `\|rit.` / `\|vb.` 带句点;`\|vb120.000000` 浮点 | 行 4、组尾列 | §2.3 |
| P11 | 混搭行头、`/*lirics*/` 拼写、双行行头、短行头 `(-ID-` | 行 7/8/17/20 等 | §3.5 + §5 |
| P12 | 和弦行 `-` / `#Fmin` / `bAmaj_46` / `G7_56 G7_34 G7_2` / 紧排多和弦 | 行 5 | §7 |
| P13 | 22 个表记定义散布 `{` 行 `\|` 之后 | 行 4 | §2.6 |
| P14 | 歌词行无 `\|`(以行头 token 推定) | 行 8/10 | §5 |
| P15 | 大跨度裸括号 `( … )`(跨约 98 列) | 行 7 @1263-1360 | 见附录 C-7 |
| P16 | `**`(笔误)、`-H`(`-` + `H`)、错位孤立 `+` | 行 12/21 等 | §4.7【新增条款】 |
| P17 | 紧排跑句 / 一拍一音 / 长持音并存;`0` 休止 | 行 7 | §1.3 + §4.1 |
| P18 | 括号单音变体 `(1)`、`(5.)`、`(7)` | 行 18 | §4.1 |
| P19 | `\@[7-6-3-]`、`\@[0]`、`\'....` | 行 21 | §4.4 + §6.2 |
| P20 | 歌词 `*l\|" "` 实际使用;内容原样输出 | 行 8/10 | §5 |
| P21 | 未闭合引号 `J&r"…` | 行 4 | §3.4 |

### 附录 C —— 实施口径(方案拟定,实施时可调整)

1. **`=X` / `====X` / `=T(4)` 的时长语义**:等号个数 n = 占用 n 个单位;`=` 后跟标记名 → 按 §8.2 自动重复语义以内容填充 n 单位,不足则截断并告警;裸 `=`(无标记名)→ 与同列机动段配对替换,无配对则按 1 单位休止并告警。
2. **`@lN`**:N = 文档总行号(1 基);从记号列起列对齐复制;本声部自身事件覆盖复制内容;`-`(§4.7)终止;复制内容按本声部音区锚点重新消解;越界告警;循环引用检出截断。
3. **`'` 与 `*` 同义**(升八度组,作用于其前音);`>`/`*` 作为音区前缀并存,乐曲区内 `*` 恒为后缀。
4. **和弦 `_` 多位数字**:按 §7 的音序定义消解为连续低音。**规则自注的"根音分别是 7,1,7,2,6"与示例能否按定义一一对应,列为实现期验证向量**;不符则回报修订本文件。
5. **力度 `s1`~`s8` 映射**:见 §2.4;缺省力度 4 = `p`。
6. **无音高打击**:非鼓组 = 任何数字同等解读为一次发音(每数字 1 单位),按乐器键位输出 `<unpitched>` + display;鼓组 = 打击谱号 + 鼓组映射 + 逐音符乐器引用。
7. **大跨度裸括号 `( … )`**:规则未定义,建议跳过并告警,不产生连线;若作者确认语义(疑为长延音/连音提示)再补入本文件。
8. **`(5.)` 中的 `.`**:按 §4.2 的「降一个八度组」前缀处理;若系附点意图,须回报修订本文件。
9. **`|` 后下划线细分的格数**:规则仅给出一例(`|__` → 每列 4 格);建议口径为「k 个下划线 → 每列细分为 2^k 格」,待更多实例验证。
10. **未闭合引号**:取至行尾并告警(实践 P21)。
11. **单位双义**(见 §1.3):`%` 类声明以列计,故 6/8 下 `%234`(4 列)跨 2 个 6/8 拍;若作者意图表达"6/8 的一拍(3 个八分)三连音",应使用机动行段(如 `\|654`)表达。此为本整理版的口径,与原文"每拍四字节"的 4/4 前提一致;如有 6/8 实践用例请回报修订。

---

*本文件由 rule_0_2.txt、rule_0_3.txt 与 part-IV.txt 实践交叉整理而成;凡【新增条款】处均为实践中已确认、原文未记载者。*
