# 《百日 finale》曲谱书写规范(整理版 rule.md)

> **定位**:本文件是 `.tymp` 曲谱书写的**唯一权威依据**,合并了规则原文的全部条款与实践中已确认的规则,并持续随规则更新而更新。
>
> **与规格文档的关系**:`tymp2musicxml_实现方案.md` 是实现本规范的技术方案,不得与本文件冲突;冲突时以本文件为准。
>
> **阅读顺序建议**:先读「一、总则」建立整体图景,再按需查「二~九」。

---

## 一、总则与结构综述

### 1.1 文件骨架

```
      左大括号前的行可以是任何内容,如位置参考。
第 1 行   「标题 by 作者」
第 2 行   「instruments:结构id0 结构id1 …」
第 3 行起 若干「组」:
     #timestamp:|||   .   .   .   |   …                ← 拍点网格行
     { |拍号 |=调号 |速度 [&gt: {吉他调弦}] …          || 【组头行】
     chords,…:                                          || 【和弦行,可选】
     …                                                  || 【机动行,可选】
     …                                                  || 【数字行】
     …                                                  || 【歌词行,可选】
     [ …                                                || 【和声行,可多个】
     ( …                                                || 【伴奏行,可多个】
     }                                                  || 【组末行】
```

- 组头元数据行**不可省略**;但若曲谱共有多列,则从第二列(第二组)起,可空置组首元数据及行首元数据,此时沿用上一组的拍号/调号/速度/乐器/音区/吉他调弦。
- 用一个以 `}` 开头的行结束本组;`}` **之后**的内容同样视为本组的元数据(组末元数据)。
- 一个文档可以包含**多个作品**(不同的「标题 by 作者」);生成 musicxml 时按不同标题/作者分别生成多个文件(见 §1.5)。

### 1.2 元数据区与 `||`

- 每一行都分为**元数据区**与**乐曲区**,分界为「组头元数据区结束后的两个 `||` 的结束点」。推荐每一行都在元数据区结尾添加 `||`,并与组头对齐,以免误记。
- **`||` 之前**是行头元数据(乐器名、音区、初始力度、特殊表记定义)。
- **`||` 之后**是乐曲区;乐曲区各行的**列号严格对齐**(见 §1.3)。
- 实践中歌词行可能没有 `||`(如 `/*lirics*/ *l|" "`),此时以行头 token(注释、`*l…`、音区等)推定元数据区终点。

### 1.3 列与时间(核心坐标系统)

- 乐曲区以**列**为书写坐标;**1 列 = 16 分音符 = 1/4 拍**(以四分音符为 1 拍)。
- **每小节列数 = 16 × 拍号分子 ÷ 拍号分母**;每拍列数 = 16 ÷ 分母。
  - 4/4 → 每拍 4 列,每小节 16 列;6/8 → 每拍 2 列,每小节 12 列。
- 规则原文所述「每一个拍子由四字节组成」以 x/4 拍号为准。
- 「**单位**」一词在规则原文中有两种用法,本文件区分如下:
  - **`[ ]` 类占位**以「**拍**」计:1 单位 = 1 拍(`[123]` 占 1 拍;`[~123]` 每个 `~` 多占 1 拍);
  - **`%` 类与特殊表记声明**以「**列**」计:1 单位 = 1 列 = 16 分音符(`%234` 占 4 列,4/4 下恰为 1 拍;6/8 下跨 2 拍,见附录 C)。
- 若某拍内等分数大于 5,该拍的书写宽度可以大于其列数。

### 1.4 动态行分类总则

- 曲谱记载中存在「**数字行**」「**机动行**」「**歌词行**」三种角色。
- **行角色一律按内容判定,不依据行头**;唯一例外是**节拍行**(`#timestamp:`,其无谱面内容,只能以行头识别)。行头仅提供元数据(乐器名、音区、定义等),不决定行的角色;组头/组尾的 `{`/`}` 属结构定界。
- **角色判定以内容与所在区域为准**:一行是数字行、机动行还是歌词行,以及主旋律行/和声行/伴奏行/和弦行之间的区分,**一律由乐曲区的内容(与所在区域、先后次序)判定**;唯一仍以行头判定的是**节拍行**(`#timestamp:`)。
  - 此前使用的行首记号(`[`、`(`、`chords:`、`/*lirics*/` 等)**不再作为角色判定依据**(旧文件中仍然出现,解析器可读但不依赖)。
  - 组界(`{` 组头行 / `}` 组末行)仍按其起始字符识别,以确定组的边界。
- **区域划分**(以组为单位):组内**第一个** `#timestamp:` 行之后、**第二个** `#timestamp:` 行之前 = **旋律/和声区**;第二个 `#timestamp:` 行之后、`}` 行之前 = **伴奏区**。
- **行数不固定、按内容动态判定**:
  - 主旋律与和声行的**行数均不限**(旋律/和声区内**第一个**数字行即主旋律,其后每个数字行依次为一个和声声部);歌词只要位于某数字行的**正下方**即可被识别。
  - 伴奏区内,每个伴奏声部的行数同样不限:一个伴奏声部**不一定要占满三个格子**,可只占一行,并临时借用旁边的行作为自己的机动行。
  - 据此,**机动行最多可以同时存在两个**(某数字行的上方一个、下方一个)。
- 两个数字行**可以紧邻**:此时只有机动行中的「单独符号」以及 `|` 开头的段落才被视为机动行的部分;否则那些数字将属于其所在的声部行。
- **机动行可以堆叠**:在上的机动行也可以有它的在上机动行,规范完全相同。

### 1.5 转换预处理与维护工具

- **补全副本**:在生成 musicxml 的时候,先将那些特殊表记和 `@l13`、`@l13^3` 之类的简化写作进行补全,生成一个**副本**,副本的扩展名改为 `.tymp`。补全的时候统一将调整行放于上方——如果那一行的那一个地方是空的;否则,如果存在非空的话,补全之前,应当先在其上方生成一个新的空行以存放其调整行。
- **维护工具**(对已有的文件):
  - 增添一个空的乐谱行;
  - 把某一个乐谱行的拍号进行改变,但不改变精度;
  - 对某一行乐谱:增加一个空的伴奏行(需要指明是哪一行);或增添一个空的和声/旋律行。
  - 上述增添行数的操作,需要对其**下方**的所有 `@lxx` 格式的数字进行相应修改(行号引用重排)。
- **多作品**:支持不同乐谱行具有不同的「标题 by 作者」,分别生成多个 musicxml 文件。

---

## 二、组头元数据

### 2.1 拍号

- 只允许出现在每一组的首行,且**曲首必须存在**(不可省略)。
- 写法 `|6/8`,意为「从 `|` 所指出的小节起,拍号为 6/8」;`|` 可省略。
- 多列曲谱中,后续组可空置,沿用上一组。

### 2.2 调号

| 表示方法 | 写法 | 含义 |
|---|---|---|
| 方法 1(视同简谱) | `\|=G`、`\|=#G`、`\|=Dm` | 完全按简谱方式转调。和弦一般跟随做完全相同的转调,**除非**加后缀 `~`(`\|=#G~` 表示和弦行不跟随转调)。 |
| 方法 2(固定变音) | `\|@[7-3-6-]` | **仅**使这些变音被固定;C 调为 `\|@[0]`。 |

- 曲首应当存在调号,**缺省值为 `|=C`**。
- 方法 2 中,`|` 依旧可以由 `\` 代替(表示其数字行在其上方,见 §6)。
- 方法 2 可以在**数字行内**进行固定变音之增减(即中途改变)。
- 旧写法 `|-7-3-6` 等价于 `|@[7-3-6-]`(兼容保留)。

### 2.3 速度记号

- **绝对速度**:`|vb180` —— 从竖线对应拍子起,单个拍子的速度。
  - 曲首应当存在绝对速度记号,**缺省 120**。
  - 实践中由生成器写作浮点形式 `|vb120.000000`,解析器须接受小数。
- **速度变化记号**(以下均以 `|` 位置为准):
  - 延音号:`|。` 或 `|vt`。
  - 渐慢/渐快:`|vi-` / `|vi+`,或 `|rit` / `|rall` / `|accel`。
  - 结束:以下三者之一结束变速:
    - `|ve` —— 渐慢或渐快结束,以当前速度继续;
    - `|vr` —— 速度变化结束,回到最初速度;
    - `|vb180` —— 速度变化结束,变为此速度。若趋势方向正确(如 120 渐快到 180),则为渐快(慢)的终点;否则终点不受影响,抵达变速终点后突变到这个速度。
    - `|vb`,同 `|vr`。
  - 实践中存在带句点的写法 `|rit.` / `|vb.`,与不带句点者同义。
- **速度变化记号仅仅可以出现在组首与组末**,但它们不一定出现在行首。

### 2.4 力度记号

- 可写作 `|ppp` 到 `|fff` 之间;也可使用 `|s1` ~ `|s10`,分别代表**十种力度**。
  - 十级规定(本文件固化,可整体替换):`s1=pppp`、`s2=ppp`、`s3=pp`、`s4=p`、`s5=mp`、`s6=mf`、`s7=f`、`s8=ff`、`s9=fff`、`s10=ffff`。
- 渐强/渐弱类比渐慢/渐快,将 `v` 替换成 `s`。
  - 支持 `|cresc` / `|dim`;或使用两个小于号、两个大于号:`<   <` / `>   >`。
  - 第二个小于号/大于号可替换成或后紧跟力度编号;**第二个小于号/大于号后如果没有后文标注,回原处**;但如果是 `|se`,不做如此操作。
  - `|sr` / `|pr` / `|sb` / `|pb` 都被识别为相同的内容。
- 曲首力度记号可以不存在,此时**按力度为 5**。
- **作用域**:力度记号若对全局生效,需要放置在《组尾》;若放置在组头,仅仅对旋律们有效用。

### 2.5 和弦把位定义

- 格式:`{D/200232}` 类推。**先设定的会被后设定的在设定的那一拍及以后推翻**。默认为常用把位表。
- **全部和弦的吉他把位应当在数据库中被提供**,以使得我们可以通过吉他把位标注来转换成音位。

### 2.6 特殊表记定义的位置【重要】

- 特殊表记的定义可以发生在**元数据行的任何位置**上,且其**定义域均为整个行**。
- 实践中 22 个定义散布在 `{` 行 `||` 之后,与 `|rit.` / `|vb.` 等混排。解析器须**整行扫描**。
- 作用域:定义在乐器行头 → 本乐器;定义在组头/组尾 → 全局。查找顺序:本乐器 → 全局。

---

## 三、行头元数据

### 3.1 乐器名

- 除了**主旋律线路**与 `[` 开头的线路外,都需要存在乐器名。
- 中文与英文都是可以接受的,用 `<>` 分割(如 `Nylon Guitar<古典尼龙吉他`)。

### 3.2 初始音区

- **现行写法** `>4`;**旧写法** `*4` / `*5` 兼容保留。
- 含义:该行**调根音 1 所在的八度**(八度编号以中央 C = C4 计)。
- 音区标注允许出现在**正文内部的数字行或机动行**:`>4|`(数字行)、`|>4|` 或 `\>4|`(机动行),代表从此处起为第四音区。
- `[` 和声行若无音区标注,继承主旋律。

### 3.3 初始力度

- 行头可含初始力度,写法同 §2.4。

### 3.4 特殊表记定义

特殊表记定义形态:

| 类 | 形态 | 说明 |
|---|---|---|
| ① 瞬时 | `x~{135}`、`x～{一三五}`、`x～{CEG}` | 被视为**一个音符**;括号内为同刻和弦(简谱数字 / 中文数字 / 绝对音名)。 |
| ② 多瞬时 | `x&"_{358}/7/3-6-"` | `_` 为空格替代,`/` 划分拍。 |
| ③ 吉他节奏型 | 同 ②,但内容使用小写 `a`~`f`;二者可以混合。 |
| ④ 特殊和弦定义 | `{Cmy/300353}` | 不产生事件,登记"和弦名→指板把位"供 ③ 查询。 |
| ⑤ 任何和弦节奏型 | `x&r"{358}7654345"` | `r` 表示内容为**和弦相对音**。 |
| ⑥ 吉他把位表注 | `X&g"xxxxxx"` | x = 1,2,3,4,5,6,代表哪一根弦(**1 为低**);声明一段按弦号书写的吉他谱型。 |

- **声明内部,引号内可以出现空格**。
- 无引号形态(`Q&{13}_{13}_{13}_`)与 `&"…"` 同义。
- **参数化形态**:`X(V)&r"V_{35}_"` —— 解析时,将输入的 `X(V)` 中 `V` 的**具体内容**走入内部(文本代入)。注意:`V` 指的是**绝对的唱名**,而不是和弦根音/三音。
- **未闭合引号容错**:定义引号未闭合时,取至引号后面的第一个空格(以空白终止)并告警。

**⑤ 类定义全内容**:

```
                 |标记名。可以是 1-4 个大/小写字母,字母数需 ≤ 被定义内容的总字符数。
                 |标记名不可以重复!不可以包含 ABCDEFG。开头与结尾不能是数字。
                 |柱式和弦。
                      |由于出现在括号内,所以代表一个音:如果 7 是和弦内音,演奏七音;
                      |如果不是,演奏 8 音,即高一个八度的和弦根音。
x&r"{358}(7/8)(A-)[56]4345"@[6-7-3-]
                    |该拍子时值被平分。
               |绝对音"A-"。  |标记变音:降 MI、LA、TI.
  |区分引号中采用和弦相对音 / 绝对音。
```

注意:
1. 和弦内音(1,3,5,7,8,9,…)总是和弦中的音;和弦外音总采用调内音。若需其他调外音插入,采用以下手段:
   - a. 在使用处写作 `x@|3-6-7-4+` / `x@|E-A-B-F+`(代表**绝对音的变音**);
   - b. 在机动行对此声部先进行变调;
   - c. 手动写一种(如分开记录大调与同主音小调)。
2. 如果在同一组和弦音之内和弦切换了,将会**重新开始节奏组**。**这条规则只适用于该组是一个"和弦",即定义处有 `r`**。
3. 数字可以替换成 A~G 来实现穿插绝对值;数字中可以存在 `+`/`-` 代表在调基础上使得该音变音,字母也可以。

### 3.5 伴奏声部行头(统一前缀)

- 伴奏声部的**每一行**(不论属于哪个声部、不论在组中第几行)都使用**完全相同的短式前缀**:

```
(-结构id-
```

  其中「结构 id」为该伴奏声部的乐器结构 id(与 `instruments:` 行中的写法一致,如 `Inst.#0`)。
- 该前缀只标识「此行属于该结构 id 的伴奏声部」;同一结构 id 的连续若干行构成该声部的行组。**行的角色(数字行/机动行/歌词行)一律由内容判定**(§1.4),不由前缀决定。
- 由此,伴奏声部的行数完全自由:可只占一行,可占多行,也可临时借用旁边的行作为自己的机动行。
- **兼容旧写法**(含义同上,历史文件仍须可读):`(Accomponiment#N:ID<Name; Initial pitch range: >4`、省略关键词的对齐式、`(-ID-后缀`、双行行头(下一行仅含乐器全名)。
- **其余行首元数据仍然适用**:初始音区 `>4`/`*4`、初始力度、特殊表记定义。
- **简洁定义** `*#1:Inst#1<Instrument1$1`:
  - 最前面的 `#1` 可以省略;
  - `<Instrument1` 可以省略;
  - `$1` 代表该行的**音色号**,同样可以省略;**未指定时率先搜索 `Inst#1` 此处代指的内容,再不出现则填充钢琴**。
  - 该定义可以完成在乐谱内部。最简便的方式是:在内部出现 `*Vln|` 并直接接续正文;**系统需要自动完成编号、指定 id 等等的操作**。
- **无任何行头** → **视为钢琴**。

**ID 独一化**:更改 ID 标识使之独一化的责任,已从新建文件工具改动到解析其并将其转换为 musicXML 的过程中。

**行头出现频次**:生成器自本版起在**每一行伴奏行**都写出行头(首组为完整形态 `(Accomponiment#N:ID<EN; Initial pitch range: >4`,续组为短式 `(-ID-`);旧文件(表头仅出现于每三行的第一行)同样须被解析。行头因此仅承担「本行乐器/音区」的元数据职责,不再承担「本行是何种行」的角色职责(§1.4)。

### 3.6 无音高打击乐器

- 无音高打击乐器行如果是**鼓组**,则映射到**鼓谱**(常用);否则任何数字**同等解读为一次发音**。

---

## 四、曲谱记载

### 4.1 事件记号

| 记号 | 含义 |
|---|---|
| `1` ~ `8` | 简谱数字(音级) |
| `0` | 从其位置开始,**停止该音符**(休止至下一事件) |
| `(1+)` | 标注括号里的内容是**同一个音符**,且**仅有这个音符**(不补空格)。**非常不建议用于数字行**;若要用,请在其他声部追加 `(/)` 占位 |
| `[123]` | 括号内内容占**同一个单位(1 拍)**,内部 n 等分 |
| `[~123]` | 每个 `~` 多占一个单位(即以 2 拍做 3 等分) |
| `{12}` | **同时演奏双音**(柱式和弦) |
| `%234` | 把**四个单位(4 列 = 4 个十六分)**三连音化;4/4 下即"一拍三连音",6/8 下跨 2 拍(见附录 C) |
| `%%234` | 把**八个单位(8 列)**三连音化 |
| `%12345`、`%1234543` | 把**八个单位(8 列)**5 / 7 连音化 |
| 特殊表记标记名 | 本身是一个事件 |
| 两个单位三连音化 | 需要参照「机动行」一节 |

**约束**:以上记号的**总占用位置数必须小于等于拍子数**,否则无法表示,需要借用机动行。

### 4.2 后缀标记

以下标识**均为后缀**,需要**占用下一个四分之一拍的谱位**;防止这么做的方式是「音符与标识前后各加一个 `_`」。

| 标记 | 含义 |
|---|---|
| `*` | 从此标识**前面的那个音**开始,上升一个八度组 |
| `.` | 从此标识**后面的那个音**开始,下降一个八度组 |
| `+` | 升号 |
| `-` | 降号 |
| `～` | 还原号 |
| `'` | **上升一个八度组**,作用同 `*`(作用于其前面的音)。实践中大量使用,原文未记载,在此补入 |

### 4.3 八度判断规则

- **纯四度(含)以内,默认连续,无需升降八度。**
- 超出纯四度时,取最近的八度;两侧距离相同时取下行的连续进行(详见实现方案 §7.5)。

### 4.4 固定变音增减

- 可以在**数字行**进行固定变音之增减。
- 标记符号为 `|@[3-6-7-]`(或 C 调:`|@[0]`)。
- 竖线依旧可以由**反斜杠**代替,以表示其数字行在其上方(见 §6.2)。
- `|@[0]` 表示清空全部固定变音(回到无固定变音)。

### 4.5 `=` 替换格式

(以规则原文例子说明)

```
（机动行）   |32 |%654    |__1234535676782171  |3/2176714/321712
（数字行）124=317=5      =  =               345====
```

- 第一个 `=` 被取代为两个音符 `32`;
- 第二个 `=` 被三连音 `%654` 取代;
- 多个 `=` 连用,只需标注起点和终点即可,但也可以连续标注,**都可读**。
- 对于第三个,机动行中的音符标记可以通过在 `|` 后紧随一个或数个下划线 `_` 来表示:**其内部的时值划分与其外部不同**。如示例 3,便将该四个空格代表的时值拆成了 16 个格子。
- 也可以省略下划线,此时将**自动平分**时值。
- 机动行内的空格**被斜杠 `/` 代替**,原因是空格会被用于区分两处内容。

### 4.6 行复制记号 `@lN`

- `@l9` 发生在**任意数字行**中,表示「**从此以后,直到下一次某事件覆盖了这种状态**,该声部均存在,且复制文档总第 9 行的内容」。
- **`@lN` 复制调整行,但是不复制其歌词**。
- `N` = **文档总行号**(1 基,含标题行与所有行)。
- 实现口径(见附录 C):从记号所在列起按列对齐复制源行内容;本声部自身事件在列上覆盖被复制内容;复制内容按**本声部**的音区锚点重新消解;越界告警忽略;循环引用检出后截断并告警。

### 4.7 平行和声复制 `@lN^M`

- 符号 `@l13^+3` 指的是:在**第 13 行的音轨的上方三度**置一个**平行和声**。
- `lN^M` 中,N 即源行;M 是一个整数(如 3、+3、-3):
  - 符号代表在音轨的**什么方向**(+ 为上、- 为下);
  - 数字代表在原音轨**上方/下方几度**的位置;
  - **此数字的绝对值必须为 7n + {3, 4, 5, 6, 8} 之一**(n 为非负整数)。
- 取音规则:
  - a. 如果某一个时刻,该行的音是**此刻的和弦内音**,那么,去寻找这一和弦**以这个音为根音的那个形式**,按照 §7「和弦诸音的定义」去寻找那一个音。
  - b. 否则,寻找那个**对于该行的调号而言的调内音**,使它在该音的上方或下方 |M| 度。
  - c. 这样的行为持续,直到下一个有效符号被检出。

### 4.8 `-` 显式结束记号

- 数字行中的 `-` 单独出现时,表示**显式结束正在进行的节奏型**(停止当前由特殊表记或 `@lN` 建立的持续状态)。
- 无进行中的状态时忽略并告警。
- 实践中该记号可能与其后的标记名连写(如 `-H`),应拆为 `-`(结束)+ `H`(标记调用)两项。

---

## 五、歌词

- 歌词行一般与曲行呈现**一对一**的样态;但歌词行中的 `-` 将成为**延音线**。
- 歌词行也可被用于调节力度符号。
- 和声行的旋律行**下方**被视为该行的歌词:分拍子地填充,**且若该拍子内并无歌词视同上文**。
- 第一个歌词行**不能以数字开启**。
- **歌词的位置**:歌词不一定和谱数字位于**同一列**,但一定位于**相同的行**。在判断的时候,应当以「**逐字句完成了对应**」作为标准——也就是,依次一一对应(不依赖列对齐)。
- **歌词分隔符**:
  - `*l""` 或 `*l|""` —— 引号内部的文字,即被识别为(**上方一行**)的歌词;
  - `*l\""` —— 如果是**下方**那行数字行的歌词,采用 `\` 代替 `|`。
- **歌词内容原样输出**:引号内的任何字符(含标点、空格、符号)原封不动输出。
- 引号未闭合时取至引号后面的第一个空格并告警。
- 旧式「裸按列歌词」写法(无 `*l` 前缀,歌词文字直接按列书写)继续兼容。
- 实践中该行头的注释拼写为 `/*lirics*/`(非 `lyrics`),解析器需接受两种拼写。

---

## 六、机动行

### 6.1 基本性质

- 机动行内的内容**被视作从属于数字行**。
- 例:`.` 在上、`6` 在下,该标记被视为「6 音,该音附带降一个八度组」;`+` 在上、`5` 在下,即「升 5 音」。
- 机动行内可以存在任意力度符号等等的标记,也可以出现前面提到的后缀、调号等。
- 机动行内的**空格被斜杠 `/` 代替**。
- 机动行中还可以看到:吉他节奏型定义、特殊和弦把位定义、一切大于等于一个瞬时的特殊表记。

### 6.2 方向语义(上指 / 下指)

- **凡涉及多个符号的标记,都必须以 `|` 开头(上指)或以 `\` 开头(下指)**,且将被视为在该对应符号对应之位置。
- **规范含义**(作者确认):
  - **`|`(上指)= 该机动内容位于数字行之上,服务于其下面一行。**
  - **`\`(下指)= 该机动内容位于数字行之下,服务于其上面一行。**
  - 二者互为相反方向;机动内容可写在数字行的上方(用 `|`)或下方(用 `\`)。
- **单独符号**(仅一个符号、无数字串)的归属:
  - 属「在下的机动行」(位于数字行下方)时,**必须位于 `\` 与 `\` 之间**;
  - **反斜杠从来不可以省略**。
  - 属「在上方的机动行」时直接书写(与数字行同列对应)。
- **两个数字行紧邻**时:只有机动行中的「单独符号」以及 `|` 开头的段落才被视作机动行的部分。
- **例(part-IV 实证)**:
  - 行 16 的 `|{42}_{31}_` 位于行 17 之上,与行 17 的 `====X` 同列配对 —— `|` 在上、服务下方 ✔
  - 行 19 的单独符号 `'`、`.'`、`|@[0]` 位于行 20(密集数字行)之上,服务行 20 ✔
  - 行 21 的 `\@[3-6-7-]`、`\ ' .' . \` 位于行 21、服务其**上方**的数字行 ✔(下指)

### 6.3 组合与限制

- 一个机动行最多同时存在两个(某数字行的上、下各一)。
- 一个伴奏声部**可能只占一行**,并**临时借用旁边的行**作为自己的机动行。
- **在上的机动行也可以有它的在上机动行,规范完全相同**(机动行可向上堆叠)。
- 数字行**自身也可以携带机动段**(如 part-IV 行 17 的 `|{72}_{61}_{5+7}`),此时该段服务其下方数字行的同列内容。

---

## 七、和弦行

- 可选行。用空行代替、或使之不存在直接接,都是可以被接受的。
- 实践中行头形如 `chords,eg.C,Am,Bmin7:`,`||` 之后为和弦序列。
- **`-` 表示该位置无和弦。**
- **和弦根音位置标注 `_`**:`G_6`、`F_46`、`Em7_34`、`Bmin7_56`、`bBmaj7_2`,其根音分别是 7、1、7、2、6。
  - `_3`、`_5` 代表**原位三和弦**。
  - `_7` 代表**原位七和弦**。
- **和弦诸音的定义**:
  - 「**3 音、5 音、7 音**」(对于七和弦)指的是和弦从根音向上的第 **2、3、4** 个音;
  - 「**2 音、4 音、6 音、7 音**」(对于三和弦)则分别指**紧邻 1 音在 1 音上方、紧邻 3 音在 3 音上方、紧邻 5 音在 5 音上方、紧邻 8 音在 8 音下方**的调内音。
- 和弦符号由「根音字母 + 可选 `#`/`b` + 品质」构成;品质实践取值含 `m`、`min`、`maj7`、`7`、`dim`、`-64` 等。多个和弦可紧排书写(`Cm    Em  G Dm_6  F`),一个 token 即一个和弦。
- **定义:`maj` 三和弦是增三和弦,而不是大三和弦。**
- **标记处的和弦替换 `H_6`**:H 是一个在第一行中按照和弦节奏型定义声明的特殊标记符号;`H_6` 代表「使用**此处的活动和弦**和 **6 所代表的和弦根音位置**替换**此行**解析得到的和弦」;这不影响其他的行。例如:和弦是 `G_46`,该行中写 `H_6`,则此行内部按照 `G_6` 分配。
- 和弦行的作用:为 ⑤ 类特殊表记提供和弦相对音的消解依据;也可输出为 MusicXML 的 `<harmony>`。

---

## 八、特殊表记展开细则

### 8.1 各类展开

| 类 | 展开 |
|---|---|
| ① | 展开为**一个**事件,内部 n 个同刻音(柱式和弦)。内容为简谱数字 → 按当前调消解;中文数字 → 同简谱;大写字母 → 绝对音。 |
| ② | 按 `/` 分拍;`_` 分隔单位;`{…}` = 该拍柱式和弦;单个数字 = 单音;`3-` 等 = 带变音音级。 |
| ③ | 同 ②,但 `a`~`f` = 琴弦名:该拍有活跃和弦 → 按把位表(④ / `{D/200232}` / 默认常用把位表)取弦上音;无和弦 → 按 `&gt:` 吉他调弦表取空弦音。 |
| ④ | **不产生事件**;登记「和弦名 → 把位」供 ③ 查询。 |
| ⑤ | 数字为**和弦相对音**(1,3,5,7,8,9… = 和弦内音;其他 = 调内音);`(7/8)` = 7 是和弦内音则奏七音、否则奏 8 音;`[56]` = 该拍时值被平分;`@[6-7-3-]` = 标记变音(降 MI/LA/TI);穿插 A~G = 绝对音;使用处 `x@|3-6-7-4+` = 临时绝对变音。和弦切换 → 节奏组重头。 |
| ⑥ | `X&g"xxxxxx"`:按弦号(1 为低弦)书写的吉他把位谱型;经把位数据库转换成音位。 |

### 8.2 自动重复语义

- **大于一个单位时长的标记将自动被重复。**
- 如果引源是一个**和弦**(定义处有 `r`),那么**每当和弦被更换,其循环将重头开始**。
- **等于一个单位时长的标记将不会被重复,而是一次性。**

### 8.3 标记名规则

- 1~4 个大/小写字母;字母数需 ≤ 被定义内容的总字符数;
- **不可以重复**;不可以包含 ABCDEFG;开头与结尾不能是数字。
- 参数化定义(如 `X(V)`)与同名普通定义可共存;调用时以 `X(3)` 形式给出参数。

---

## 九、伴奏声部与乐器实装

### 9.1 声部排列

- 伴奏声部行数**灵活**:不一定要占满三个格子,按内容随时判断一行的内容是否(暂时)属于一个独立的伴奏行。
- 一个伴奏声部可能**只占一行**,并临时借用旁边的行作为自己的机动行。
- **一个伴奏声部如果没有在行首找到乐器定义,视为钢琴。**
- 最严谨的方式是在每一行行首都存在乐器定义行,代表此行的乐器。

### 9.2 数据结构实装清单

需在数据结构中实装:歌名作者、力度记号、速度记号、变速记号、和弦行、无音高打击乐器。
- 无音高打击乐器行:是**鼓组** → 映射到**鼓谱**(常用);否则任何数字**同等解读为一次发音**。

### 9.3 ID 独一化

- 独一化责任在**解析并转换为 musicXML 的过程中**。
- 建议口径:先做 XML NCName 清洗(`#`、`:`、`(` 等替换为 `-`),再对重名追加 `-2`、`-3`…;谱内就地定义(`*Vln|`)使用自动编号。

---

## 十、附录

### 附录 A —— part-IV 实践实例索引(供实现参考)

| # | 实践写法 | 位置(part-IV.txt) |
|---|---|---|
| 1 | `'` 升八度组后缀 | 行 6/7/17/18/19/21/24 等 |
| 2 | `>4` 音区前缀(与 `*3`/`*4` 并存) | 行 7/17/20/23/26/29 |
| 3 | `@l7` / `@l9` | 行 18/24/29/30 |
| 4 | 管道柱式和弦 `\|{42}_{31}_` | 行 16/17 |
| 5 | 管道括号等分 `\|[{31'}_{27'}_{16-}_{75'}_]` | 行 17 |
| 6 | 参数化定义与调用 `X(V)&r"…"` / `X(3)` | 行 4 / 行 17-18 |
| 7 | 无引号定义 `Q&{13}_{13}_{13}_` | 行 4 |
| 8 | 定义内 `'`/`.`/`8`/`0`/`_`/`(7/8)`/首导根音 `1_` | 行 4 |
| 9 | 等号与标记名连用 `====X`、`=T(4)`、`=R` | 行 17-18 |
| 10 | `\|rit.` / `\|vb.` 带句点;`\|vb120.000000` 浮点 | 行 4、组尾列 |
| 11 | 混搭行头、`/*lirics*/` 拼写、双行行头、短行头 `(-ID-` | 行 7/8/17/20 等 |
| 12 | 和弦行 `-` / `#Fmin` / `bAmaj_46` / `G7_56 G7_34 G7_2` / 紧排多和弦 | 行 5 |
| 13 | 22 个表记定义散布 `{` 行 `\|` 之后 | 行 4 |
| 14 | 歌词行无 `\|`(以行头 token 推定) | 行 8/10 |
| 15 | 大跨度裸括号 `( … )`(跨约 98 列) | 行 7 @1263-1360 |
| 16 | `**`(笔误)、`-H`(`-` + `H`)、错位孤立 `+` | 行 12/21 等 |
| 17 | 紧排跑句 / 一拍一音 / 长持音并存;`0` 休止 | 行 7 |
| 18 | 括号单音变体 `(1)`、`(5.)`、`(7)` | 行 18 |
| 19 | `\@[7-6-3-]`、`\@[0]`、`\'....` | 行 21 |
| 20 | 歌词 `*l\|" "` 实际使用;内容原样输出 | 行 8/10 |
| 21 | 未闭合引号 `J&r"…` | 行 4 |

### 附录 B —— 实施口径(方案拟定,实施时可调整)

1. **`=` 段的时长与配对**:等号段 = 连续 `=` 的最大串;段替换内容 = 段首列同列的机动段(上方 `|段` / 下方 `\段`),时值 = 等号个数 × 1 列(每 `=` 1 列),无配对则告警并按休止处理。**`=` 后跟随的标记名针对其后的部分,与等号段没有任何关系**(如 `====X` 的 `X`、`=T(4)` 的 `T(4)` 均为段后的普通标记调用)。连续 `=` 号的开头那个 `=` 永远满足且仅满足以下三种情况之一:a. 正上方为 `|`;b. 正下方为 `\`;c. 是标记一段等号结束的那个 `=`。由此即可区分「两次单 `=`」与「省略中间段的长等号段」。
2. **`@lN`**:N = 文档总行号(1 基);从记号列起列对齐复制;本声部自身事件覆盖复制内容;到 `-` 或**任意其余符号**(任何显式事件)终止;复制内容按本声部音区锚点重新消解;越界告警;循环引用检出截断;**不复制歌词**(§4.6)。
3. **`@lN^M` 平行和声**:在「补全副本」(§1.5)阶段展开为一条调整行(写于源行上方,必要时先插入新空行);展开时逐列按 §4.7 的 a/b 规则取音,直到下一个有效符号;**插入新空行后,对副本中所有行数引用(`@lN`/`@lN^M`)进行补正**。
4. **`'` 与 `*` 同义**(升八度组,作用于其前音);`>`/`*` 作为音区前缀并存,乐曲区内 `*` 恒为后缀。
5. **和弦 `_` 低音位置**:`_3`、`_5` = 原位三和弦;`_7` = 原位七和弦;`_6`、`_56` = **三音为低音**;`_46`、`_34` = **五音为低音**;`_2` = **七音为低音**。规则自注的"根音分别是 7,1,7,2,6"(C 大调下即低音 B、C、B、D、A)按此映射与示例一一对应,列为实现期验证向量。

6. **力度 `s1`~`s10` 映射**:见 §2.4(十级力度);缺省力度 = **5(mp)**。
7. **无音高打击**:非鼓组 = 任何数字同等解读为一次发音(每数字 1 单位),按乐器键位输出 `<unpitched>` + display;鼓组 = 打击谱号 + 鼓组映射 + 逐音符乐器引用。
8. **大跨度裸括号 `( … )`**:从属于**人声段**时,段内内容在补全副本的过程中**转移至伴奏声部**,括号内内容**不分配歌词**;否则跳过并告警,不产生连线。
9. **`(5.)` 中的 `.`**:按 §4.2 的「降一个八度组」前缀处理;**附点意图未被实现、也不会被实现**。
10. **`|` 后下划线细分的格数**:规则仅给出一例(`|__` → 每列 4 格);口径为「k 个下划线 → 每列细分为 2^k 格」,待更多实例验证。
11. **未闭合引号**:取至**引号后面的第一个空格**(以空白终止)并告警。
12. **单位双义**(见 §1.3):`%` 类声明以列计,故 6/8 下 `%234`(4 列)跨 2 个 6/8 拍;若作者意图表达"6/8 的一拍(3 个八分)三连音",应使用机动行段——**机动段在上方用 `|654`、在下方用 `\654`**。此为本文件的口径,与原文"每拍四字节"的 4/4 前提一致;如有 6/8 实践用例请回报修订。
13. **歌词逐字句对应**(§5):不按列对齐,按字句顺序依次一一对应;与"拍内无歌词视同上文"并存——同一拍内的多个字句依次附着该拍内各事件,超出则延续到后续事件。
14. **`maj` 三和弦 = 增三和弦**(§7):和弦解析中 `maj` 品质按增三和弦消解(根音上方大三度 + 增五度);`maj7` 保持不变(大三七和弦)。
15. **`H_6` 替换**(§7):只影响标记调用所在行的和弦消解;其余行不受影响。
16. **维护工具的行号重排**(§1.5):增行后其下方所有 `@lN`/`@lN^M` 中的 N 值需要相应加 1(或按实际插入行数递增)。
17. **多作品输出**(§1.5):同一文档中不同「标题 by 作者」的乐谱行,转换时拆分为多个 .musicxml 文件。

---

*本文件随规则原文持续更新;与实现方案冲突时以本文件为准。*
